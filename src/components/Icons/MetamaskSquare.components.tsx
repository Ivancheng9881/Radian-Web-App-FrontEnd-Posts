import config from "../../commons/config";

const MetamaskSquareIcon = () => <img className="rd-icon-custom" src={`${config.assets.cdn}/icon/metamask_square.png`} />;

export default MetamaskSquareIcon;